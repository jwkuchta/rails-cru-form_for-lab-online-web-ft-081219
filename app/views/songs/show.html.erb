<h1>Song#show</h1>
<p>Find me in app/views/song/show.html.erb</p>

<h1><%= @song.name %></h1><br>
<%= link_to @song.genre.name, genre_path(@song.genre.id) if @song.genre %>
<%= link_to @song.artist.name, artist_path(@song.artist.id) if @song.artist %>

